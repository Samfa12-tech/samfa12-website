# Product content

This directory holds reviewed source notes and product copy for the generated public pages. It is excluded from the Pages artifact. Run `npm run build:content` after changing it, and `npm run check:content` to detect stale generated HTML.
